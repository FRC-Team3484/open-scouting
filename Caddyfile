open-scouting-dev.nfoert.dev {
    encode gzip zstd

    handle_path /api_docs* {
        rewrite * /docs{uri}
        reverse_proxy backend:8000
    }

    handle_path /api/* {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:3000
    }
}
