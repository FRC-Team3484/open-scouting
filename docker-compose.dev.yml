services:
  backend:
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"

  frontend:
    volumes:
      - ./frontend:/app
      - /app/node_mudiles
    command: npm run dev -- --host
    ports:
      - "5173:5173"